# ── Go binaries ──────────────────────────────────────────────
chaincode/shipment/vendor/
chaincode/shipment/shipment

# ── Fabric binary downloads (install-fabric.sh output) ───────
network/bin/
network/builders/
network/config/
network/install-fabric.sh

# ── Generated crypto / channel artifacts (recreated by network.sh) ──
network/organizations/peerOrganizations/
network/organizations/ordererOrganizations/
network/organizations/fabric-ca/
network/channel-artifacts/
network/log.txt
network/*.tar.gz

# ── CA server runtime data (generated at runtime) ─────────────
organizations/

# ── Duplicate configtx files (canonical copy is network/configtx/) ──
network/configtx/core.yaml
network/configtx/orderer.yaml

# ── Node.js ───────────────────────────────────────────────────
client/node_modules/
client/package-lock.json
client/wallet/

# ── Connection profile (generated by gen_conn.py) ─────────────
client/connection-manufacturer.json

# ── OS / IDE ──────────────────────────────────────────────────
.DS_Store
*.swp
*.swo
*~
.vscode/
.idea/

# ── Logs ──────────────────────────────────────────────────────
*.log

REPORT.pdf
REPORT.aux
REPORT.out
REPORT.log
compile1.log
client/wallet/
